1. Delete or rename the old file named `requirement`.
2. Upload `requirements.txt` exactly with this filename.
3. Replace the existing `road_safety_dissertation_coding.py` with the corrected version.
4. Replace `README.md` with the corrected README in this package.
5. Replace or upload `road_safety_dissertation_coding.ipynb`.
6. Do not upload the raw Department for Transport datasets if they are too large. The README explains where the data should be placed locally.

After uploading, check that the repository contains exactly these core files:

```text
README.md
requirements.txt
road_safety_dissertation_coding.py
road_safety_dissertation_coding.ipynb
```

A marker should then be able to run:

```bash
pip install -r requirements.txt
python road_safety_dissertation_coding.py
```
